name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  docker:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout repository
        uses: actions/checkout@v3

      # Step 2: Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: iot91355
          password: dckr_pat_IicwsRPM4AMAKciJAfhDb_Z-ZyU

      # Step 3: Build Docker image
      - name: Build Docker image
        run: docker build -t iot91355/my-html-site:latest .

      # Step 4: (Optional) Test Docker container
      - name: Test Docker container
        run: docker run --rm iot91355/my-html-site:latest ls /usr/share/nginx/html

      # Step 5: Push Docker image to Docker Hub
      - name: Push Docker image
        run: docker push iot91355/my-html-site:latest
